import { HTMLAttributes, ReactNode } from 'react'; import { classNames } from './utils'; export const Badge=({variant='default',icon,dot,children,className,...p}:HTMLAttributes<HTMLSpanElement>&{variant?:'default'|'success'|'warning'|'danger'|'info'|'neutral'|'featured';icon?:ReactNode;dot?:boolean})=>{const s={default:'bg-slate-100 text-slate-700 dark:bg-slate-800 dark:text-slate-200',success:'bg-green-100 text-green-800 dark:bg-green-950 dark:text-green-200',warning:'bg-amber-100 text-amber-900 dark:bg-amber-950 dark:text-amber-200',danger:'bg-red-100 text-red-800 dark:bg-red-950 dark:text-red-200',info:'bg-blue-100 text-blue-800 dark:bg-blue-950 dark:text-blue-200',neutral:'bg-gray-100 text-gray-700 dark:bg-gray-800 dark:text-gray-200',featured:'bg-purple-100 text-purple-800 dark:bg-purple-950 dark:text-purple-200'};return <span {...p} className={classNames('inline-flex items-center gap-1.5 rounded-full px-2.5 py-1 text-xs font-semibold',s[variant],className)}>{dot&&<span aria-hidden className="h-1.5 w-1.5 rounded-full bg-current"/>}{icon}{children}</span>};
export const StatusBadge=({value}:{value:string})=>{const v=value.toUpperCase();const variant=v.includes('ACTIVE')||['READY','PAID','COMPLETED','AVAILABLE'].includes(v)?'success':v.includes('PENDING')||v==='PREPARING'?'warning':v.includes('FEATURE')?'featured':v.includes('CANCEL')||v.includes('DISABLE')||v.includes('SUSPEND')?'danger':'neutral';return <Badge variant={variant} dot><span className="sr-only">Status: </span>{value}</Badge>};
