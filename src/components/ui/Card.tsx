import { HTMLAttributes, PropsWithChildren } from 'react'; import { classNames } from './utils'; type P=PropsWithChildren<HTMLAttributes<HTMLDivElement>>;
export const Card=({children,className,...p}:P&{variant?:'default'|'interactive'|'muted'|'elevated'})=>{const v=(p as any).variant||'default';const {variant,...rest}=p as any;return <div {...rest} className={classNames('min-w-0 overflow-hidden rounded-2xl border border-slate-200 bg-white p-4 dark:border-slate-800 dark:bg-slate-900 sm:p-5',v==='interactive'&&'transition hover:-translate-y-0.5 hover:shadow-md focus-within:ring-4 focus-within:ring-emerald-600/15',v==='muted'&&'bg-slate-50 dark:bg-slate-800',v==='elevated'&&'shadow-md',className)}>{children}</div>};
export const CardHeader=({className,...p}:P)=><div {...p} className={classNames('mb-4 space-y-1',className)}/>; export const CardTitle=({className,...p}:P)=><div {...p} className={classNames('text-lg font-semibold text-slate-950 dark:text-white',className)}/>; export const CardDescription=({className,...p}:P)=><div {...p} className={classNames('text-sm text-slate-600 dark:text-slate-300',className)}/>; export const CardContent=({className,...p}:P)=><div {...p} className={classNames('min-w-0',className)}/>; export const CardFooter=({className,...p}:P)=><div {...p} className={classNames('mt-5 flex flex-wrap items-center gap-2',className)}/>;
