import { useCallback, useEffect, useRef, useState } from 'react'; import { Outlet, useLocation, useNavigate } from 'react-router-dom'; import toast from 'react-hot-toast'; import { useAppDispatch,useAppSelector } from '../../hooks'; import { logout, restoreSession } from '../../store/authSlice'; import { toggleDarkMode } from '../../store/uiSlice'; import { onSessionExpired, resetSessionExpiredBroadcast } from '../../utils/sessionEvents'; import { Drawer } from '../ui/Drawer'; import Header from './Header'; import Sidebar,{SidebarContent} from './Sidebar';
const AppShell=()=>{const dispatch=useAppDispatch();const navigate=useNavigate();const location=useLocation();const user=useAppSelector(s=>s.auth.user);const darkMode=useAppSelector(s=>s.ui.darkMode);const[open,setOpen]=useState(false);const triggerRef=useRef<HTMLElement|null>(null);const close=useCallback(()=>{setOpen(false);requestAnimationFrame(()=>triggerRef.current?.focus())},[]);const performLogout=useCallback(()=>{dispatch(logout());resetSessionExpiredBroadcast();setOpen(false);navigate('/login',{replace:true,state:{loggedOut:true}})},[dispatch,navigate]);useEffect(()=>setOpen(false),[location.pathname]);useEffect(()=>onSessionExpired(()=>{dispatch(logout());toast.error('Your session expired. Please sign in again.');navigate('/login',{replace:true,state:{sessionExpired:true,from:location.pathname}})}),[dispatch,location.pathname,navigate]);useEffect(()=>{dispatch(restoreSession())},[dispatch]);if(!user)return <Outlet/>;return <div className="min-h-[100dvh] bg-[var(--color-background)] text-[var(--color-text-primary)]"><div className="mx-auto flex min-h-[100dvh] w-full max-w-[var(--container-wide)]"><Sidebar user={user} onLogout={performLogout}/><Drawer open={open} onClose={close} title="Navigation"><SidebarContent user={user} onNavigate={close} onLogout={performLogout}/></Drawer><div className="flex min-w-0 flex-1 flex-col"><Header user={user} darkMode={darkMode} onOpenMenu={()=>{triggerRef.current=document.activeElement as HTMLElement;setOpen(true)}} onToggleTheme={()=>dispatch(toggleDarkMode())} onLogout={performLogout}/><main id="main-content" className="min-w-0 flex-1 px-3 py-4 pb-[max(1.25rem,env(safe-area-inset-bottom))] sm:px-5 sm:py-5 lg:px-8"><div className="mx-auto w-full max-w-[var(--container-content)]"><Outlet/></div></main></div></div></div>};export default AppShell;
