import { Bell, LogOut, Menu, Moon, Store, Sun } from 'lucide-react'; import { ReactNode } from 'react'; import { SessionUser } from '../../types'; import { Button } from '../ui/Button';
const Header=({user,darkMode,onToggleTheme,onLogout,onOpenMenu,title='Operations Workspace',subtitle,actions}:{user:SessionUser;darkMode:boolean;onToggleTheme:()=>void;onLogout:()=>void;onOpenMenu:()=>void;title?:string;subtitle?:string;actions?:ReactNode})=><header className="sticky top-0 z-[var(--z-header)] min-h-[var(--header-height)] border-b border-slate-200 bg-white/90 px-3 py-2 backdrop-blur-xl dark:border-slate-800 dark:bg-slate-950/90 sm:px-5 sm:py-3"><div className="flex min-w-0 items-center justify-between gap-2"><div className="flex min-w-0 flex-1 items-center gap-2"><Button size="icon" variant="ghost" aria-label="Open navigation" onClick={onOpenMenu} className="shrink-0 lg:hidden"><Menu aria-hidden className="h-5 w-5"/></Button><div className="min-w-0"><p className="truncate text-sm font-semibold sm:text-base">{title}</p>{subtitle&&<p className="hidden truncate text-xs text-slate-500 sm:block">{subtitle}</p>}<p className="truncate text-[11px] text-slate-500 md:hidden">{user.restaurantId?`Restaurant ${user.restaurantId}`:'Platform'}</p></div></div><div className="flex min-w-0 shrink-0 items-center justify-end gap-1">{actions?<div className="hidden min-w-0 flex-wrap justify-end gap-2 xl:flex">{actions}</div>:null}<span className="hidden max-w-[14rem] items-center gap-2 truncate rounded-xl border border-slate-200 px-3 py-2 text-sm dark:border-slate-700 md:inline-flex"><Store aria-hidden className="h-4 w-4 shrink-0 text-emerald-600"/><span className="truncate">{user.restaurantId?`Restaurant ${user.restaurantId}`:'Platform'}</span></span><Button size="icon" variant="ghost" aria-label="Notifications" className="hidden sm:inline-flex"><Bell aria-hidden className="h-5 w-5"/></Button><Button size="icon" variant="ghost" aria-label={darkMode?'Use light theme':'Use dark theme'} onClick={onToggleTheme}>{darkMode?<Sun aria-hidden className="h-5 w-5"/>:<Moon aria-hidden className="h-5 w-5"/>}</Button><Button variant="ghost" aria-label="Log out of Tapro" onClick={onLogout} leftIcon={<LogOut aria-hidden className="h-5 w-5"/>} className="hidden sm:inline-flex">Log out</Button><Button size="icon" variant="ghost" aria-label="Log out of Tapro" onClick={onLogout} className="sm:hidden"><LogOut aria-hidden className="h-5 w-5"/></Button></div></div></header>; export default Header;
